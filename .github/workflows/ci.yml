name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Configure Git identity for tests
        run: |
          git config --local user.name "VANILAAAAAAAA"
          git config --local user.email "xchen247@uw.edu"
          git config --global user.name "VANILAAAAAAAA"
          git config --global user.email "xchen247@uw.edu"
          git config --local user.name
          git config --local user.email
          git var GIT_AUTHOR_IDENT
          git var GIT_COMMITTER_IDENT

      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m pytest
      - run: python -m graph_harness_maintain pipeline local-rc --ci
